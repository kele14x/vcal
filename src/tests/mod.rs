// Test submodule split — see individual files for each
// operator / feature area. This file only declares the
// submodules; shared imports are intentionally NOT placed
// here so each submodule stays self-contained.

mod arithmetic;
mod arrays;
mod bitwise;
mod casts;
mod concat;
mod conditional;
mod deep_nesting;
mod display;
mod integer_real_decls;
mod limits;
mod literals;
mod logical;
mod lvalue;
mod parser_ast;
mod real;
mod real_functions;
mod relational;
mod repl;
mod selects;
mod shift;
mod strings;
mod system_tasks;
mod variables;
